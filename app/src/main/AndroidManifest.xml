<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment2">
        <activity android:name="com.example.quizapp.QuizRules"></activity>
        <activity android:name="com.example.quizapp.ChangePassword" />
        <activity
            android:name="com.example.quizapp.HomePage"
            android:label="@string/title_activity_home_page"
            android:theme="@style/Theme.Assignment2.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="com.example.quizapp.Register"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="com.example.quizapp.Login"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>